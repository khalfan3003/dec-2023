



// let h2 = document.querySelector('h2')

// console.log(h2);

// what is text inside it?
// textContent(brainless) , innerText(brainful) , innerHTML

// getters
// console.log(h2.innerText);    // only the UI part (ignores what is in css)
// console.log(h2.textContent);  // shows everything except tags (even css ignored part too)
// console.log(h2.innerHTML);    // shows html + text + css everything


// setters
// h2.innerText = "<h1>my name is don</h1>"
// h2.textContent = "<h1> my name is don </h1>"
// h2.innerHTML = "<h1> my name is don </h1>"




